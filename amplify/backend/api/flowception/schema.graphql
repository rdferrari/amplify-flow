type Mapstory @model {
  id: ID!
  title: String!
  description: String!
  owner: String!
  locations: [Location] @connection(name: "MapstoryLocations")
}

type Location @model {
  id: ID!
  title: String!
  mapstory: Mapstory @connection(name: "MapstoryLocations")
  notes: [Note] @connection(name: "LocationNotes")
}

type Note @model {
  id: ID!
  note: String
  location: Location @connection(name: "LocationNotes")
}
